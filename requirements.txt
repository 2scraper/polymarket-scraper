# Polymarket Scraper — Python dependencies

# Core HTTP (async, used by Playwright and Pyppeteer versions)
httpx>=0.27.0

# Sync HTTP (used by Selenium version)
requests>=2.31.0

# Browser automation (install the one you need)
playwright>=1.44.0        # Primary — run: playwright install chromium
selenium>=4.20.0          # Alternative
pyppeteer>=2.0.0          # Alternative (headless Chrome via CDP)

# Optional: for running Selenium with auto-managed ChromeDriver
webdriver-manager>=4.0.0
